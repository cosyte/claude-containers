{
  "_comment": "Declarative plugin bake-in. The entrypoint unions extraKnownMarketplaces and enabledPlugins into ~/.claude/settings.json; Claude Code installs/syncs them on startup (idempotent). Ships empty so the image is generic. Add your own marketplaces/plugins and rebuild, or mount your own plugins.json / settings.json at runtime.",

  "_example": {
    "extraKnownMarketplaces": {
      "my-marketplace": {
        "source": { "source": "git", "url": "https://github.com/me/my-marketplace.git" },
        "autoUpdate": true
      }
    },
    "enabledPlugins": {
      "my-plugin@my-marketplace": true
    }
  },

  "extraKnownMarketplaces": {},
  "enabledPlugins": {}
}
