# Local config / secrets, never commit
.env
# Real scenario files live with the deployed compose (outside this repo); if one
# is dropped here, keep it out of git. The tracked template is *.conf.example.
scenarios/*.conf

# Build / runtime artifacts
*.log
workspace/

# npm task-runner shim (zero deps): npm ci leaves only a marker here
node_modules/
